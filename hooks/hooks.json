{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const fs=require('fs');try{if(!fs.readdirSync('docs/specs').some(f=>f.endsWith('.md')))console.log('No spec found in docs/specs/ — consider running /spec before implementing.')}catch{console.log('No spec found in docs/specs/ — consider running /spec before implementing.')}\""
          }
        ]
      }
    ]
  }
}
